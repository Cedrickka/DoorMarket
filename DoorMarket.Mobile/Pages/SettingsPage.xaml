<?xml version="1.0" encoding="utf-8" ?>
<local:LocalizedContentPage xmlns:local="clr-namespace:DoorMarket.Mobile.Localization"     x:Class="DoorMarket.Mobile.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:components="clr-namespace:DoorMarket.Mobile.Components"
    xmlns:markup="clr-namespace:DoorMarket.Mobile.Markup"
    xmlns:models="clr-namespace:DoorMarket.Mobile.Models"
    NavigationPage.HasNavigationBar="False">

    <Grid RowDefinitions="Auto,*">
        <Grid
            BackgroundColor="{StaticResource DoorBlue}"
            ColumnDefinitions="Auto,*,Auto"
            ColumnSpacing="16"
            Padding="{OnPlatform iOS='24,56,24,20', Default='24,50,24,20'}">
            <ImageButton Clicked="OnBackClicked">
                <ImageButton.Source>
                    <FontImageSource FontFamily="fa-solid-900" Glyph="{x:Static models:IconGlyphs.Back}" Color="White" Size="16" />
                </ImageButton.Source>
            </ImageButton>
            <VerticalStackLayout Grid.Column="1" Spacing="8" VerticalOptions="Center">
                <Label FontFamily="Inter-Bold" FontSize="22" Text="{markup:Translate SettingsTitle}" TextColor="White" />
                <Label FontFamily="Inter-Regular" FontSize="13" Text="{markup:Translate SettingsSubtitle}" TextColor="#DCE9F8" />
            </VerticalStackLayout>
            <Border
                Grid.Column="2"
                HorizontalOptions="End"
                VerticalOptions="Center"
                HeightRequest="34"
                WidthRequest="58"
                BackgroundColor="#26FFFFFF"
                StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10" />
                </Border.StrokeShape>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnLanguageTapped" />
                </Border.GestureRecognizers>
                <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="2" HorizontalOptions="Center" VerticalOptions="Center">
                    <Image x:Name="CurrentLanguageImage" HeightRequest="12" WidthRequest="18" />
                    <Label
                        Grid.Column="1"
                        FontFamily="Inter-SemiBold"
                        FontSize="11"
                        Text="â–¾"
                        TextColor="White"
                        VerticalTextAlignment="Center" />
                </Grid>
            </Border>
        </Grid>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="24,16,24,24" Spacing="16">
                <components:DmCard Margin="0">
                    <VerticalStackLayout Spacing="12">
                        <Label Style="{StaticResource SectionTitleLabelStyle}" Text="{markup:Translate SettingsLanguage}" />
                        <Picker x:Name="LanguagePicker" />
                    </VerticalStackLayout>
                </components:DmCard>

                <components:DmCard Margin="0">
                    <VerticalStackLayout Spacing="12">
                        <Label Style="{StaticResource SectionTitleLabelStyle}" Text="{markup:Translate SettingsAppearance}" />
                        <Picker x:Name="ThemePicker" Title="{markup:Translate SettingsTheme}" />
                    </VerticalStackLayout>
                </components:DmCard>

                <components:DmCard Margin="0">
                    <VerticalStackLayout Spacing="12">
                        <Label Style="{StaticResource SectionTitleLabelStyle}" Text="{markup:Translate SettingsNotifications}" />
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                            <Label FontFamily="Inter-Regular" FontSize="14" Text="{markup:Translate SettingsOrdersNotifications}" VerticalOptions="Center" />
                            <Switch x:Name="OrdersNotifSwitch" Grid.Column="1" OnColor="{StaticResource DoorOrange}" />
                        </Grid>
                    </VerticalStackLayout>
                </components:DmCard>

                <components:DmCard Margin="0">
                    <VerticalStackLayout Spacing="10">
                        <Label Style="{StaticResource SectionTitleLabelStyle}" Text="{markup:Translate SettingsVersion}" />
                        <Label x:Name="VersionLabel" Style="{StaticResource MutedLabelStyle}" />
                    </VerticalStackLayout>
                </components:DmCard>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</local:LocalizedContentPage>

